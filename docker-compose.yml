services:
  api:
    build:
      context: ./src/Blogger.Api
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
    restart: unless-stopped
